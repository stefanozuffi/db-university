------ IN CLASS ------
4. Selezionare tutti gli studenti che si sono iscritti nel 2021 (734) 
    SELECT `name` FROM `students` 
    WHERE `enrolment_date` LIKE '2021-%';

5. Selezionare tutti i corsi che non hanno un sito web (676) 
    SELECT `name`,`cfu` FROM `courses`
    WHERE `website` IS NULL;

6. Selezionare tutti gli insegnanti che hanno un numero di telefono (50) 
    SELECT `name`,`surname` FROM `teachers`
    WHERE `phone` IS NOT NULL;

7. Selezionare tutti gli appelli d'esame dei mesi di giugno e luglio 2020 (2634) 
    SELECT `date`, `hour`,`location` FROM `exams`
    WHERE `date` >= '2020-06-01' AND `date` <= '2020-07-31';

8. Qual è il numero totale degli studenti iscritti? (5000)
    SELECT COUNT(id) AS `total_students` FROM `students`;



------ ASSIGNMENT --------

1. Selezionare tutti gli studenti nati nel 1990 (160)
    SELECT `name`, `surname` FROM `students`
    WHERE YEAR(`date_of_birth`) = '1990';


2. Selezionare tutti i corsi che valgono più di 10 crediti (479)
    SELECT * FROM `courses`
    WHERE `cfu`> 10;


3. Selezionare tutti gli studenti che hanno più di 30 anni (3913?)
        SELECT `name`, `surname` FROM `students`
        WHERE `date_of_birth` < CURRENT_DATE - INTERVAL 30 YEAR; 

    #or (3914?):
        SELECT `name`, `surname` FROM `students`
        WHERE `date_of_birth` < NOW() - INTERVAL 30 YEAR; 


4. Selezionare tutti i corsi del primo semestre del primo anno di un qualsiasi corso di laurea (286)
    SELECT * FROM `courses`
    WHERE `year` = 1 AND `period`='I semestre';

5. Selezionare tutti gli appelli d'esame che avvengono nel pomeriggio (dopo le 14) del 20/06/2020 (21)
    SELECT * FROM `exams`
    WHERE `hour` > '14:00:00' AND `date` = '2020-06-20';

6. Selezionare tutti i corsi di laurea magistrale (38)
    SELECT * FROM `degrees`
    WHERE `level` = 'magistrale';

7. Da quanti dipartimenti è composta l'università? (12)
    SELECT COUNT(id) AS `depart_number` FROM `departments`;

8. Quanti sono gli insegnanti che non hanno un numero di telefono? (50)
    SELECT COUNT(id) AS `num_teachers_with_a_phone` FROM `teachers`
    WHERE `phone` IS NOT NULL;


